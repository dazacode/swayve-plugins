name: swayve_plugin_sdk
description: >-
  The pure-Dart contract between the Swayve music client and a Swayve plugin:
  provider interfaces, normalized models, host facilities and errors.
version: 0.1.0
publish_to: none
repository: https://github.com/dazacode/swayve-plugins
issue_tracker: https://github.com/dazacode/swayve-plugins/issues

environment:
  sdk: ^3.6.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
